// Rust автоматически ожидает, что подмодули <branches_if_else.rs> и <variables.rs> будут находиться в папке с таким же именем,
// как и родительский модуль, в данном случае src/modules.

pub mod branches_if_else;  // Указывает, что существует модуль `mod1` в папке `modules`
pub mod variables;  // Указывает, что существует модуль `mod2` в папке `modules`
